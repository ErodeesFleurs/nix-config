# Wallpapers Module
# Provides wallpaper assets for other modules to reference (no runtime configuration).
{ ... }:

{
  # Intentionally minimal module:
  # Wallpaper files live in this directory and are referenced by other modules (e.g., stylix, hyprland).
  # No options or config are exposed here.
}
